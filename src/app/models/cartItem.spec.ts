import { CartItemComponent } from './../components/shopping-cart/cart/cart-item/cart-item.component';
import { CartItem } from './cartItem';

describe('Cart', () => {
  it('should create an instance', () => {
    expect(new CartItemComponent()).toBeTruthy();
  });
});
